from django.contrib import admin
# ShopSubscription (payments/models.py) is a legacy stub model not wired into the
# subscription flow. No admin registration needed here.
# The real subscription admin is in apps/subscriptions (handled via AdminUserViewSet).
