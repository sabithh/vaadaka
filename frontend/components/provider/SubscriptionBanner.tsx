'use client';

// SubscriptionBanner is no longer used — Vaadaka uses commission-based billing (2% per completed rental).
// This component is kept as a stub to avoid import errors from any legacy references.

export default function SubscriptionBanner() {
    return null;
}
